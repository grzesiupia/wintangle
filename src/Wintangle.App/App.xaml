<Application x:Class="Wintangle.App.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Wintangle.App">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <!-- Default theme; swapped at runtime by App.ApplyTheme
                     (index 0 only — Controls.xaml below is never replaced). -->
                <ResourceDictionary Source="Themes/Dark.xaml" />
                <!-- Shared control styles; brushes resolve via DynamicResource
                     from the active theme dictionary. -->
                <ResourceDictionary Source="Themes/Controls.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Application.Resources>
</Application>
